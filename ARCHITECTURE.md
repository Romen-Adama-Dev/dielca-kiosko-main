# 🏗️ Arquitectura Visual - Dielca Kiosco

## Clean Architecture - Diagrama de Capas

```
╔═══════════════════════════════════════════════════════════════════╗
║                         UI LAYER (Presentación)                    ║
║  ┌────────────────────────────────────────────────────────────┐  ║
║  │ SCREENS                    │ COMPONENTS                     │  ║
║  │ • SplashScreen.js          │ • FabButton.js                 │  ║
║  │ • CategoriasScreen.js      │ • CartPanel.js                 │  ║
║  │ • ProductosScreen.js       │ • QtyModal.js                  │  ║
║  │ • ResumenScreen.js         │ • DetailModal.js               │  ║
║  │ • ConfirmacionScreen.js    │ • SearchModal.js               │  ║
║  │                            │                                 │  ║
║  │ ROUTER                     │ IDLE TIMER                      │  ║
║  │ • Router.js                │ • IdleTimer.js                  │  ║
║  └────────────────────────────────────────────────────────────┘  ║
╚═══════════════════════════════════════════════════════════════════╝
                              ↓ depende de ↓
╔═══════════════════════════════════════════════════════════════════╗
║                   APPLICATION LAYER (Servicios)                    ║
║  ┌────────────────────────────────────────────────────────────┐  ║
║  │ • ProductoService.js                                        │  ║
║  │   - getCategorias()                                         │  ║
║  │   - getProductosByCategoria()                               │  ║
║  │   - getProductoDetail()                                     │  ║
║  │                                                              │  ║
║  │ • CartService.js                                            │  ║
║  │   - addItem()                                               │  ║
║  │   - removeItem()                                            │  ║
║  │   - getTotal()                                              │  ║
║  │   - clear()                                                 │  ║
║  └────────────────────────────────────────────────────────────┘  ║
╚═══════════════════════════════════════════════════════════════════╝
                              ↓ depende de ↓
╔═══════════════════════════════════════════════════════════════════╗
║                      DOMAIN LAYER (Entidades)                      ║
║  ┌────────────────────────────────────────────────────────────┐  ║
║  │ ENTIDADES                  │ INTERFACES                     │  ║
║  │ • Producto.js              │ • IProductoRepository.js       │  ║
║  │ • Categoria.js             │                                 │  ║
║  │ • CartItem.js              │ (Define contratos)              │  ║
║  └────────────────────────────────────────────────────────────┘  ║
╚═══════════════════════════════════════════════════════════════════╝
                              ↑ implementa ↑
╔═══════════════════════════════════════════════════════════════════╗
║                INFRASTRUCTURE LAYER (Implementación)               ║
║  ┌────────────────────────────────────────────────────────────┐  ║
║  │ REPOSITORIES               │ DATA                           │  ║
║  │ • StaticProductoRepository │ • categorias.js                │  ║
║  │ • ApiProductoRepository    │ • productos.js                 │  ║
║  │ • InMemoryCartRepository   │ • productDetails.js            │  ║
║  │                            │ • icons.js                      │  ║
║  └────────────────────────────────────────────────────────────┘  ║
╚═══════════════════════════════════════════════════════════════════╝
```

---

## Flujo de Datos

```
┌─────────────┐
│   Usuario   │
│   (Click)   │
└──────┬──────┘
       │
       ↓
┌─────────────────────────────────────────────────────┐
│  UI COMPONENT                                        │
│  • CartPanel.js                                      │
│  • Detecta click en "Añadir al carrito"            │
└──────┬──────────────────────────────────────────────┘
       │ llama método
       ↓
┌─────────────────────────────────────────────────────┐
│  APPLICATION SERVICE                                 │
│  • CartService.addItem(producto, cantidad)          │
│  • Valida y ejecuta lógica de negocio              │
└──────┬──────────────────────────────────────────────┘
       │ usa
       ↓
┌─────────────────────────────────────────────────────┐
│  DOMAIN ENTITY                                       │
│  • CartItem (producto, cantidad, precio)            │
└──────┬──────────────────────────────────────────────┘
       │ persiste en
       ↓
┌─────────────────────────────────────────────────────┐
│  INFRASTRUCTURE REPOSITORY                           │
│  • InMemoryCartRepository.addItem()                 │
│  • Almacena en memoria (o localStorage)            │
└─────────────────────────────────────────────────────┘
       │
       ↓ notifica cambio
┌─────────────────────────────────────────────────────┐
│  UI UPDATE                                           │
│  • CartService._notify()                            │
│  • Listeners actualizan UI                          │
│  • Panel del carrito se re-renderiza                │
└─────────────────────────────────────────────────────┘
```

---

## Inyección de Dependencias

```javascript
// main.js - Composition Root

┌──────────────────────────────────────┐
│  1. CREAR REPOSITORIOS                │
│                                       │
│  const productoRepo = config.useApi   │
│    ? new ApiProductoRepository()      │
│    : new StaticProductoRepository()   │
│                                       │
│  const cartRepo =                     │
│    new InMemoryCartRepository()       │
└──────────────┬────────────────────────┘
               │ inyectar en
               ↓
┌──────────────────────────────────────┐
│  2. CREAR SERVICIOS                   │
│                                       │
│  const productoService =              │
│    new ProductoService(productoRepo)  │
│                                       │
│  const cartService =                  │
│    new CartService(cartRepo)          │
└──────────────┬────────────────────────┘
               │ inyectar en
               ↓
┌──────────────────────────────────────┐
│  3. CREAR UI COMPONENTS               │
│                                       │
│  const cartPanel =                    │
│    new CartPanel(cartService, router) │
│                                       │
│  const productosScreen =              │
│    new ProductosScreen(               │
│      router,                          │
│      productoService,                 │
│      qtyModal,                        │
│      detailModal                      │
│    )                                  │
└───────────────────────────────────────┘
```

---

## Estructura de Archivos CSS

```
styles/
│
├── main-modular.css ← ENTRY POINT (importa todo)
│
├── base/ ────────────── FUNDAMENTOS
│   ├── variables.css     (Design Tokens)
│   ├── reset.css         (Normalize)
│   ├── animations.css    (Keyframes)
│   └── utilities.css     (Helpers)
│
├── layout/ ──────────── ESTRUCTURA
│   ├── app-shell.css     (Containers, Screens)
│   └── grid.css          (Grids Responsive)
│
├── components/ ──────── BLOQUES REUTILIZABLES
│   ├── buttons.css       (Buttons, FABs)
│   ├── cards.css         (Product/Category Cards)
│   ├── modals.css        (Modal System)
│   └── cart.css          (Cart Panel)
│
└── pages/ ───────────── PANTALLAS ESPECÍFICAS
    ├── splash.css        (Bienvenida)
    ├── categorias.css    (Catálogo)
    ├── productos.css     (Productos)
    ├── resumen.css       (Checkout)
    └── confirmacion.css  (Success)
```

---

## Navegación entre Pantallas

```
┌───────────────┐
│  SPLASH       │ (2 segundos)
│  splash.css   │
└───────┬───────┘
        │ timer
        ↓
┌───────────────┐
│  CATEGORÍAS   │ ← [Inicio]
│  categorias   │   (home)
│    .css       │
└───────┬───────┘
        │ seleccionar
        ↓ categoría
┌───────────────┐
│  PRODUCTOS    │ ← [Atrás]
│  productos    │
│    .css       │
└───────┬───────┘
        │ añadir
        │ productos
        ↓ al carrito
┌───────────────┐
│  RESUMEN      │ ← [Volver] [Confirmar]
│  resumen.css  │
└───────┬───────┘
        │ confirmar
        │ pedido
        ↓
┌───────────────┐
│ CONFIRMACIÓN  │ (auto-redirect)
│ confirmacion  │     5 seg
│    .css       │      ↓
└───────────────┘   [Inicio]
```

---

## Componentes Flotantes (Z-Index)

```
┌──────────────────────────────────────────┐
│                                          │  Z: 9999
│      #add-feedback (Toast)               │
│                                          │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│  #search-modal, #detail-modal, etc.      │  Z: 900
│                                          │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│         #cart-panel                      │  Z: 500
│                                          │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│       #cart-overlay                      │  Z: 400
│                                          │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│  .cart-fab  .search-fab                  │  Z: 200
│                                          │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│       CONTENIDO PRINCIPAL                │  Z: 1
│                                          │
└──────────────────────────────────────────┘
```

---

## Ciclo de Vida - Usuario Típico

```
1. LLEGADA
   ┌─────────────────┐
   │ Usuario toca    │
   │ la pantalla     │
   └────────┬────────┘
            ↓
   ┌─────────────────┐
   │ Splash Screen   │ (2 seg)
   │ Logo animado    │
   └────────┬────────┘
            ↓

2. NAVEGACIÓN
   ┌─────────────────┐
   │ Categorías      │
   │ Grid de cards   │
   └────────┬────────┘
            ↓ click categoría
   ┌─────────────────┐
   │ Productos       │
   │ Grid de items   │
   └────────┬────────┘
            ↓ click producto
            
3. SELECCIÓN
   ┌─────────────────┐
   │ Detail Modal    │
   │ Ver detalle     │
   └────────┬────────┘
            ↓ click info (i)
   ┌─────────────────┐
   │ Qty Modal       │
   │ Elegir cantidad │
   └────────┬────────┘
            ↓ confirmar
            
4. CARRITO
   ┌─────────────────┐
   │ Cart Panel      │
   │ Revisar items   │
   │ Ver total       │
   └────────┬────────┘
            ↓ checkout
            
5. CHECKOUT
   ┌─────────────────┐
   │ Resumen         │
   │ Confirmar pedido│
   └────────┬────────┘
            ↓ enviar
            
6. CONFIRMACIÓN
   ┌─────────────────┐
   │ Éxito           │
   │ # de pedido     │
   │ (5 seg timer)   │
   └────────┬────────┘
            ↓ auto
   ┌─────────────────┐
   │ RESET           │
   │ Volver a inicio │
   └─────────────────┘
```

---

## Sistema de Eventos

```javascript
// Pattern: Observer/PubSub

┌─────────────────────────────────────┐
│  CartService (Subject)               │
│                                      │
│  _listeners = []                     │
│                                      │
│  onChange(callback) {                │
│    _listeners.push(callback)         │
│  }                                   │
│                                      │
│  _notify() {                         │
│    _listeners.forEach(fn => fn())    │
│  }                                   │
└──────────┬──────────────────────────┘
           │ notifica a
           ↓
┌──────────────────────────────────────┐
│  OBSERVERS (Listeners)                │
│                                       │
│  • CartPanel                          │
│    - Re-renderiza lista               │
│    - Actualiza total                  │
│                                       │
│  • FabButton                          │
│    - Actualiza badge                  │
│    - Anima botón                      │
│                                       │
│  • ResumenScreen                      │
│    - Actualiza resumen                │
└───────────────────────────────────────┘
```

---

## Helpers & Utilities - Árbol de Decisión

```
┿━ ¿Necesitas manipular el DOM?
   ┠─ SÍ → import { getById, query, createElement } from './utils/helpers.js'
   ┗─ NO → ↓

┿━ ¿Necesitas formatear valores?
   ┠─ SÍ → import { formatCurrency, formatNumber } from './utils/helpers.js'
   ┗─ NO → ↓

┿━ ¿Necesitas debounce/throttle?
   ┠─ SÍ → import { debounce, throttle } from './utils/helpers.js'
   ┗─ NO → ↓

┿━ ¿Necesitas constantes?
   ┠─ Timings → import { UI_CONSTANTS } from './constants.js'
   ┠─ Mensajes → import { MESSAGES } from './constants.js'
   ┠─ Rutas → import { ROUTES } from './constants.js'
   ┗─ IDs → import { ELEMENT_IDS } from './constants.js'
```

---

## Performance Budget

```
┌────────────────────────────────────────┐
│  MÉTRICA          OBJETIVO    ACTUAL   │
├────────────────────────────────────────┤
│  First Paint      < 1s        0.8s ✅  │
│  Interactive      < 2s        1.5s ✅  │
│  JS Bundle        < 100KB     ~50KB ✅ │
│  CSS Bundle       < 50KB      ~30KB ✅ │
│  Images           < 200KB     ~80KB ✅ │
│  Total Page       < 500KB     ~160KB ✅│
└────────────────────────────────────────┘
```

---

## Testing Strategy (Futuro)

```
┌─────────────────────────────────────────┐
│  UNIT TESTS                             │
│  • Services (ProductoService, etc.)     │
│  • Helpers (formatCurrency, etc.)       │
│  • Domain Entities                      │
├─────────────────────────────────────────┤
│  INTEGRATION TESTS                      │
│  • Repositories con mock data           │
│  • Router navigation                    │
├─────────────────────────────────────────┤
│  E2E TESTS                              │
│  • User flow completo                   │
│  • Add to cart → checkout               │
└─────────────────────────────────────────┘
```

---

## Deployment Pipeline (Propuesto)

```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│   DEV   │ →  │  BUILD  │ →  │  TEST   │ →  │  DEPLOY │
└─────────┘    └─────────┘    └─────────┘    └─────────┘
    │              │              │              │
    │              │              │              │
    ↓              ↓              ↓              ↓
  Local        Webpack/        Unit +         Production
  Server        Vite          E2E Tests        Server
 (Python)     (minify)       (Playwright)     (Apache/
                                                Nginx)
```

---

**Documentación creada:** 2026-02-20  
**Versión:** 2.1.0  
**Arquitectura:** Clean Architecture  
